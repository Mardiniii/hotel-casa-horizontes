<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2"> 
      <h2 class="text-center" style="margin-top: 150px;">Iniciar sesión como administrador de contenido</h2>     
      <div class="text-center"><%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="field form-group">
                <%= f.label :email, "Correo Electrónico:" %>
                <%= f.email_field :email, autofocus: true %>
              </div>
              <div class="field form-group">
                <%= f.label :password, "Contraseña:" %>
                <%= f.password_field :password, autocomplete: "off" %>
              </div>
              <div class="actions form-group">
                <%= f.submit "Iniciar sesión", class: "btn btn-success" %>
              </div>
            <% end %></div>
    </div>
  </div>
</div>
